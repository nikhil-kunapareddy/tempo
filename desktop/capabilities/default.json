{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Tempo's window loads the local sidecar directly; no extra IPC surface is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
